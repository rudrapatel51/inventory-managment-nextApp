"use client";
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("click on add to cart")}>
        Add to card
      </button>
    </div>
  );
};

export default AddToCart;
